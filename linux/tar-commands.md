# Basic Tar commands

Example extracting a .tar.gz of postman to `/opt/postman`

```shell
# Extract to destination
tar -xzvf postman-linux-x64.tar.gz /opt/postman
```

**Notes**

The flags used in the example: 

```
x = Extract
z = Use gzip
v = Verbose
f = file to extract
```
